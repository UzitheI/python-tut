# -*- coding: cp1252 -*-

#this ensures that the encoding is done in cp1252 format
#by default a python file is derived as UTF-8 which means that if we are using a different encoding that the default we must use a header#

